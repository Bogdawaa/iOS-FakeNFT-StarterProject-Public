import UIKit

extension UIImage {
    static let profileTab = UIImage(named: "profile") ?? UIImage()
    static let catalogTab = UIImage(named: "catalog") ?? UIImage()
    static let basketTab = UIImage(named: "basket") ?? UIImage()
    static let statisticsTab = UIImage(named: "statistics") ?? UIImage()
}
